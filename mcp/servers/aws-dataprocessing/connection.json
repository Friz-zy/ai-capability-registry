{
  "mcpServers": {
    "aws-dataprocessing": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "mcp/aws-dataprocessing-mcp-server"
      ]
    }
  }
}
