{
  "mcpServers": {
    "aws-appsync": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-e",
        "AWS_SECRET_ACCESS_KEY",
        "-e",
        "AWS_SESSION_TOKEN",
        "mcp/aws-appsync-mcp-server"
      ]
    }
  }
}
