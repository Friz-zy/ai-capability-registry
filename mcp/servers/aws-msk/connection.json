{
  "mcpServers": {
    "aws-msk": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-e",
        "AWS_SECRET_ACCESS_KEY",
        "-e",
        "AWS_SESSION_TOKEN",
        "mcp/aws-msk-mcp-server"
      ]
    }
  }
}
