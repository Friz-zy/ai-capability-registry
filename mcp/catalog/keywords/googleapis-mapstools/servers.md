# googleapis-mapstools MCP Servers

Select only servers that directly match the current request. If a server matches, read its `mcp/servers/<server>/SKILL.md` and use it only within MCP safety rules.

- **mcp** (`com.googleapis.mapstools-mcp`) — `hosted_https`, `trusted`: Ground your AI applications with trusted geospatial data from Google Maps.
