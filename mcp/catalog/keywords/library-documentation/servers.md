# library-documentation MCP Servers

Select only servers that directly match the current request. If a server matches, read its `mcp/servers/<server>/SKILL.md` and use it only within MCP safety rules.

- **Context7 MCP** (`context7`) — `hosted_https`, `trusted`: Trusted hosted MCP for retrieving current library documentation and code examples.
