# openzeppelin-cairo-contracts MCP Servers

Select only servers that directly match the current request. If a server matches, read its `mcp/servers/<server>/SKILL.md` and use it only within MCP safety rules.

- **OpenZeppelin Cairo Contracts** (`openzeppelin-cairo-contracts`) — `hosted_https`, `trusted`: Software Development MCP server by OpenZeppelin.
