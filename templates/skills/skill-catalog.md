# Skill Catalog

This is a generated human-readable view. Edit `enabled` and `keywords` in `skill-catalog.d/*.yaml`.

Total: {{total}} skills across {{category_count}} categories
Enabled: {{enabled}}; Disabled: {{disabled}}

{{sections}}
