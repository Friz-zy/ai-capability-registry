# Threat Modeling Workflow

Workflow id: `threat-modeling`

Use this workflow to model assets, trust boundaries, attack paths, mitigations, and residual risks.

## Files

- Manifest: `workflows/security-compliance/threat-modeling/workflow.yaml`
- Common schemas: `workflows/templates/schemas/`
- Common artifact templates: `workflows/templates/artifacts/`

## Coordinator Notes

This workflow is design-oriented and risk-modeling focused. Use `security-review` when validating an implementation, release, or existing system.

Follow the manifest for stage order, role delegation, gates, and acceptance criteria.
